<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.ia.project</groupId>
    <artifactId>DynamicStudyPlanner</artifactId>
    <version>2.0.1</version>
    <name>Dynamic Study Planner</name>
    <description>Dynamic Study Planner</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <properties>
        <java.version>21</java.version>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-brave</artifactId>
        </dependency>
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>7.4</version>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.17</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
            <version>3.1.1</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j-core</artifactId>
            <version>8.10.1</version>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <!--
            Estado compartilhado entre replicas (etapa 06b, achados E1 e E6).

            Ate a etapa 06 o limite de taxa vivia num Caffeine local a cada instancia: com N
            replicas o limite declarado virava N x o valor configurado — demonstrado com dois
            processos reais, 10 requisicoes aceitas onde a politica declara 5.

            O Redis e opcional em tempo de execucao: sem `api.shared-state.store=redis` a
            aplicacao usa os armazenamentos locais e AVISA no log que nao pode ser replicada.
            Nao ha auto-configuracao de conexao no boot padrao, entao a dependencia nao impoe
            um Redis a quem roda uma instancia so.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-pool2</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j-redis</artifactId>
            <version>8.10.1</version>
        </dependency>
        <!--
            Redis de verdade, em processo, so para teste.

            O que se quer verificar no achado E1 e que DUAS replicas apontando para o MESMO servidor
            enxergam o mesmo balde. Um dubl� em memoria compartilhado pelos dois lados provaria
            apenas que o duble e compartilhado. Este artefato sobe um redis-server real numa porta
            livre, entao o teste exercita o compare-and-swap do bucket4j sobre o protocolo real —
            e roda em qualquer maquina, sem exigir Redis instalado nem conteiner.
        -->
        <dependency>
            <groupId>com.github.codemonstur</groupId>
            <artifactId>embedded-redis</artifactId>
            <version>1.4.3</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
                Attaches benchmarks/java as an extra TEST source root.

                The benchmark harness (docs/revisao-ag/03-validacao.md) must never ship inside the
                application artifact, so it is compiled at test scope: spring-boot-maven-plugin
                repackages only main classes, and test classes are excluded from the jar by
                construction. Keeping the sources in their own top-level directory rather than under
                src/test/java also makes the separation visible on disk.
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.6.1</version>
                <executions>
                    <execution>
                        <id>add-benchmark-test-source</id>
                        <phase>generate-test-sources</phase>
                        <goals>
                            <goal>add-test-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>benchmarks/java</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                    <source>24</source>
                    <target>24</target>
                    <compilerArgs>--enable-preview</compilerArgs>
                </configuration>
            </plugin>
            <!--
                Cobertura de testes. Antes da etapa 01b nenhuma ferramenta de cobertura estava
                configurada: o numero existia apenas quando alguem invocava o JaCoCo a mao.

                O piso do goal `check` e um catraca, nao uma meta: esta fixado logo abaixo do valor
                medido no commit que o introduziu, para que a cobertura possa subir livremente e nao
                possa cair sem que alguem decida baixar o piso de proposito, no mesmo commit e a
                vista do revisor. Os numeros e a justificativa estao em
                docs/qualidade/01b-correcao-testes.md.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>3.6.0</version>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>10.21.1</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <configLocation>config/checkstyle/checkstyle.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failOnViolation>true</failOnViolation>
                    <violationSeverity>error</violationSeverity>
                    <includeTestSourceDirectory>true</includeTestSourceDirectory>
                </configuration>
                <executions>
                    <execution>
                        <!-- Roda no validate: um erro de estilo aparece em segundos, antes de
                             compilar e testar, em vez de no fim de um build de minutos. -->
                        <id>checkstyle-validation</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.15</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <!--
                        Piso de cobertura: uma CATRACA, nao uma meta.

                        Os valores sao a medicao exata do estado atual — 6290 de 6905 instrucoes e
                        379 de 520 ramos —, nao um numero escolhido com folga. A regra e uma so, no
                        elemento BUNDLE (o projeto inteiro somado); nao existe regra por classe.

                        Historico dos pisos:
                          etapa 01b  0,77 / 0,55  medicao era 0,79 / 0,57 — folga proposital, para
                                                  o gate nascer sem reprovar trabalho em andamento
                          etapa 04b  0,9109 / 0,7288  medicao exata, sem folga

                        POR QUE SEM FOLGA. A cobertura subiu de 0,77 para 0,91 em quatro etapas, e o
                        piso ficou onde estava — uma catraca que nao trava nao e catraca. Com o piso
                        colado na medicao, remover UMA instrucao coberta reprova o build: 6289/6905
                        da 0,91079, abaixo de 0,9109.

                        ISSO E SEGURO PORQUE A MEDICAO E DETERMINISTICA. Verificado na etapa 04b com
                        tres execucoes limpas consecutivas: 6290/6905 e 379/520 nas tres, identicas
                        instrucao por instrucao. A aleatoriedade do AG e sempre semeada nos testes, e
                        RandomProviderIsolation restaura a fonte original entre eles.

                        COMO MEXER NISTO. Ao acrescentar codigo coberto, o piso NAO se atualiza
                        sozinho: rode `mvn clean verify`, leia os numeros em target/site/jacoco/
                        jacoco.csv e suba os dois valores. Ao acrescentar codigo sem teste, o build
                        reprova — e essa e a funcao. Baixar o piso para fazer um build passar deve
                        ser uma decisao explicita, registrada na mensagem do commit; e o unico
                        movimento que desfaz a garantia.
                    -->
                    <execution>
                        <id>check-coverage-floor</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <!--
                                            Cuidado ao subir estes numeros: o JaCoCo TRUNCA a razao
                                            antes de comparar, nao arredonda. A medicao real e
                                            0,91469, que ele le como 0,9146 — um piso de 0,9147
                                            reprova um build que nao regrediu em nada. Tire o valor
                                            do relatorio com precisao, nao de um printf arredondado.
                                        -->
                                        <limit>
                                            <counter>INSTRUCTION</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.9215</minimum>
                                        </limit>
                                        <!--
                                            0,7386 ate a etapa 05b. A conversao dos tres objetivos
                                            de fitness para forEach (achado F8) removeu tres lacos
                                            for, e com eles tres ramos que ESTAVAM cobertos. Tirar
                                            ramos cobertos de um conjunto coberto em 74 % abaixa a
                                            razao: 390/527 = 0,7401 virou 387/524 = 0,7385. Nao ha
                                            teste a menos — a cobertura de instrucoes subiu no
                                            mesmo commit, de 0,9135 para 0,9145.
                                        -->
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.7445</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
